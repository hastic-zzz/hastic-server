matrix:
  include:
    - language: python
      python: 3.6.6
      before_script:
        - cd analytics
          pip install -r requirements.txt
          apt-get update && apt-get install -y apt-utils gnupg curl make g++ git
      script:
        - python -m unittest discover
notifications:
  email: false